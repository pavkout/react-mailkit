import { Meta, ArgsTable } from '@storybook/addon-docs/blocks';

import { EPreview } from '@react-mailkit/core';

<Meta title='Email Design System/Standard Head components/ePreview' />

# EPreview

This tag allows you to set the preview that will be displayed in the inbox of the recipient.

EPreview doesn't support any attribute.

### Import

```js
import { EPreview } from '@react-mailkit/core';
```

### Examples

```html
<EMail>
  <EHead>
    <EPreview>Hello MJML</EPreview>
  </EHead>
  <EBody>
    <ESection>
      <EColumn>
        <EText> Hello World! </EText>
      </EColumn>
    </ESection>
  </EBody>
</EMail>
```

### Props

<ArgsTable of={EPreview} />
