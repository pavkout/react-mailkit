{
  "name": "@react-mailkit/live",
  "version": "0.0.1",
  "main": "dist/index.cjs.js",
  "module": "dist/index.esm.js",
  "browser": "dist/index.umd.js",
  "types": "dist/types/index.d.ts",
  "license": "MIT",
  "sideEffects": false,
  "scripts": {
    "clean": "rimraf dist",
    "type-check": "tsc --noEmit",
    "type-check:watch": "yarn type-check -- --watch",
    "build:types": "tsc --emitDeclarationOnly",
    "build:ts": "NODE_ENV=production rollup -c",
    "build": "yarn clean && yarn build:types && yarn build:ts",
    "dev:ts": "yarn clean && rollup -cw",
    "dev": "yarn clean && yarn build:types && yarn dev:ts"
  },
  "dependencies": {
    "@react-mailkit/core": "0.0.1"
  }
}
