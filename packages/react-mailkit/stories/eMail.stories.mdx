import { Meta, ArgsTable } from '@storybook/addon-docs';

import { EMail } from '../src/browser';

<Meta title="Email Design System/Components/eEmail" />

# EMail

A react-mailkit document starts with a `<EMail>` tag, it can contain only EHead and EBody tags. Both have the same purpose of head and body in a HTML document.

### Props

<ArgsTable of={EMail} />
